package org.usfirst.frc.team4213.autonomous;

public class AutoMission1 extends Mission {

	// The Go Straight For X Feet Mission

	public void execute() {
		stepOne();
	}

	private void stepOne() {
		// Drive Straight for 15 fee

		// if(distanceTraveled < 15 feet) {
		// double angle = driveTrain.getAngle();
		// // double Kp = 0.15;
		//
		// figure out rou PID loop here for going stright.
		//
		// double Kp = SmartDashboard.getNumber("Kp", .15);
		// System.out.println("Drive angle: " + (angle));
		// driveTrain.autoDrive(0, -angle * Kp);
		// } else {
		// Step1 = COMPLETE;
		// }
	}

}
